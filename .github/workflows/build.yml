name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Clean generated files
        run: |
          rm -f .flutter-plugins .flutter-plugins-dependencies
          rm -rf .dart_tool build
          flutter clean
          flutter pub get

      - name: Build APK with Gradle Stacktrace
        run: flutter build apk --debug -- --stacktrace
